{
  "name": "syncwatch",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter syncwatch-app tauri dev",
    "build": "pnpm --filter syncwatch-app tauri build",
    "server:dev": "pnpm --filter syncwatch-server dev",
    "server:deploy": "pnpm --filter syncwatch-server deploy"
  }
}
